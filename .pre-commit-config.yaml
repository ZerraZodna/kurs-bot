minimum_pre_commit_version: '3.0.0'

default_language_version:
  python: python3.12

repos:
  - repo: local
    hooks:
      - id: timezone-ban-check
        name: Ban direct timezone imports
        entry: grep -E -rn --include="*.py" "(import datetime|from datetime import timezone|from zoneinfo)" src/ || true
        language: system
        pass_filenames: false
        exclude: src/core/timezone.py

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: debug-statements
      - id: requirements-txt-fixer

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.8
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: pip-audit
        name: Pip-audit security scan
        entry: bash -c 'pip install pip-audit >/dev/null 2>&1 || true; pip-audit --ignore-vuln CVE-2026-4539'
        language: system
        pass_filenames: false
        always_run: true
        verbose: true

# Offline - requires internet (legacy)
#  - repo: https://github.com/pre-commit/mirrors-safety
#
#  - repo: https://github.com/Yelp/detect-secrets
#    rev: v1.5.0
#    hooks:
#      - id: detect-secrets
#        args: [--baseline, .secrets.baseline]

#  - repo: https://github.com/ibidem/markdownlint-cli2-pre-commit
#    rev: v0.12.0
#    hooks:
#      - id: markdownlint-cli2
#        args: [-c, pyproject.toml]

#  - repo: https://github.com/pre-commit/pre-commit-hooks
#    rev: v4.6.0
#    hooks:
#      - id: no-executable-mode
#        exclude: ^scripts/
