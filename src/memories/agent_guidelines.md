# Agent Guidelines (short pointer)

This file intentionally does not duplicate `AI.md`.

See `AI.md` at the repository root for the authoritative agent onboarding and rules.

If you need a longer human reference, add it under `docs/` and link it from `AI.md`.
