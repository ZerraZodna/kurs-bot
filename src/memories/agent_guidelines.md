# Agent Guidelines (short pointer)

This file intentionally does not duplicate shared agent instructions.

See `AGENTS.md` at the repository root for the authoritative agent workflow and contribution guide.

If you need a longer human reference, add it under `docs/`.
