# Memories

This package will contain persistent memory handling for user conversational
state, TTL memories, and related helpers.

Planned modules:
- `service.py` — high-level MemoryService
- `manager.py` — DB helpers for CRUD operations
