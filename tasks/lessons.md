# Lessons (append-only pointer)

This file is intentionally minimal to avoid duplicating `AI.md`.

After a user-corrected bug, add a single-line lesson entry and a one-sentence root-cause/fix summary.

- See `AI.md` for the full self-improvement loop rules.

Example entry format:
- 2026-02-15: Extracted duplicate parsing into helper — reduced function size.
